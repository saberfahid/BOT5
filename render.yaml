services:
  - type: worker
    name: multi-bot-worker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_manager.py
    autoDeploy: true
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: RENDER
        value: "true"
